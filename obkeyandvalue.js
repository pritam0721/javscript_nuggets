const person = {
    name:"john",
    age:26,
    status:"not married",
}

const result = Object.entries(person);

for (const [first,second] of result) {
  console.log(first,second);
    return first;
}
